spring:
  datasource:
    url: jdbc:mysql://localhost:3306/u_rep?useSSL=false&serverTimezone=UTC
    username: root
    password: user_passworD1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
    open-in-view: true
  mvc:
    hiddenmethod:
      filter:
        enabled: true